"use strict";

const assert = require("chai").assert
const Dataserve = require("../src/index");

describe("Dataserve Tests", function() {
    it("Single write key test", function(done) {
        done();
    });
});
