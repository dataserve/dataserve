"use strict"

const Module = require("../module");

class User extends Module {

    add(hooks) {
        super.add(hooks);
    }

    lookup(hooks) {
        super.lookup(hooks);
    }
    
}

module.exports = User;
